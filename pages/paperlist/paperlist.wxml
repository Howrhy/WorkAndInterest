<view class='container'>
  <block wx:for="{{paper_list}}" wx:key="index">
    <navigator url="../setpaper/setpaper?paperId={{item.id}}">
      <view class='text-content'>{{item.name}}</view>
    </navigator>
  </block>
</view>